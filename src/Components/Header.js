import React from 'react';
import { Container } from 'react-bootstrap';

const Header = (props) => {
  return (
  	<Container>
	    <h1 className='header'>
	    	Expense Tracker
	    </h1>
    </Container>
  )
}

export default Header;