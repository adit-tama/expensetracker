import React from 'react';
import { Button } from 'react-bootstrap';

const Save = (props) => {
  return (
    <div>
    	<Button variant='success'> Save Record </Button>
    </div>
  )
}

export default Save;